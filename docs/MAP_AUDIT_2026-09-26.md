# `/map` design, quality and data-fidelity audit — 2026-09-26

**Target:** `http://localhost:8011/map?lat=38.090526&lng=13.392334&zoom=12.00` (Palermo, zoom 12)
**Branch:** `feature/map-srs-implementation` (working tree with uncommitted changes to `map-v2.js`, `map-v2.css`, `map_layers.py`, `routers/api.py`, `routers/enrichment.py`, and the `theme_overrides/map_v2.html` template)
**Tooling:** Playwright MCP (Chromium), direct `psql` against the local `aecs4u-stats` PostGIS database, `curl` against the running API.
**Screenshots:** [`docs/map-audit-2026-09-26/`](map-audit-2026-09-26/)

---

## 1. Summary

This audit records the **pre-remediation baseline** observed on 2026-09-26. For the layers and tiles that loaded successfully, rendered feature IDs matched the aecs4u-stats database for administrative boundaries, OMI zones, postal zones, and Veneto parcels. Failed tiles caused missing features, so this does not mean the full viewport was always represented faithfully.

The main problems at baseline were:

- **Parcel selection is unreliable.** The reference lookup behind every parcel click does a full scan of 4.68 M rows. It times out whenever the row isn't near the start of the heap.
- **Three features are broken in the UI but look enabled:**
  - In dark mode, popup text is effectively invisible (about 1.04:1 contrast).
  - The attribute table renders underneath the map canvas.
  - The live POI layer always returns zero results with the default category selection.
- **Municipality search only matches exact names**, because SQL parameters are bound in the wrong order. It then falls back to public Nominatim as you type, which that service's usage policy forbids.
- **The requested location has no cadastral data at all.** aecs4u-stats only has Veneto parcels. The page never says so, and keeps prompting "Zoom in to see cadastral parcels".
- **Tiles that fail on a cold cache are never retried.** They leave silent holes: 17 of 238 hazard polygons were missing in the audited view.

> **Remediation:** this file records both the pre-remediation baseline and the follow-up implementation. See [§8](#8-remediation-log) for current status. H6 was revised after re-checking showed that the apparent self-neighbour was a different polygon with the same reference.

The severity counts below describe the **baseline**, not the remaining backlog. Status after the index migration and a full browser re-run on 2026-09-26 (details in §8 and §8.1):

- **Fixed and browser-verified:** C1–C4, H3, H5, H7, H8, M1, M3, M5–M9, M12, L1, L3–L6, L8–L10.
- **Partly fixed:** H2 (tile warming), H4 (source backfill), H6 (reference-only links), M4 (desktop help note vs attribution; mobile "Sign In" wrap), M10 (language button name), M11 (initial layer order).
- **Open outside this repo:** L2 (font preload in aecs4u-theme).
- **New finding from verification:** municipality search returns 503 while tiles are loading under database load (§8.1).
- **Regressions from the remediation, found and fixed during review and verification:** municipality names dropped (H1), Civil Protection bulletin empty (M12), idle render loop overwriting every status message (M9/L10), and table pagination hidden (M1). See §8.1.
- **Tests:** 50 focused contract tests pass.

| Severity | Count |
|---|---|
| Critical | 4 |
| High | 8 |
| Medium | 12 |
| Low | 10 |

---

## 2. Scope and method

1. Loaded the target URL at 1440×900 in both themes, then at 768 px and 375×812. Captured the accessibility tree, console, network, and performance entries.
2. Hooked the MapLibre constructor with an init script (`window.__auditMap`) to read the live style, layer order, paint properties and rendered features (`queryRenderedFeatures`) without changing app code.
3. **Data fidelity:** for each overlay, I compared the set of rendered feature IDs in the exact viewport bounds against `ST_Intersects(geom, viewport)` in aecs4u-stats. I did this at Palermo (the target) and at Verona z16/z17, because parcel coverage only exists in Veneto.
4. Exercised every control: search, layers, the live layers (POI, fires, bulletin, sales, auctions), parcel click/detail/adjacent/copy/save/clear, attribute table, locate, share, Google Maps/Street View, theme toggle, keyboard navigation, and URL edge cases.
5. Traced each defect to source and confirmed root causes with `EXPLAIN ANALYZE` or direct API calls where possible.

The source state was the uncommitted `feature/map-srs-implementation` work tree named above. The exact commit SHA, Chromium version, and database snapshot identifier were not recorded, which limits exact reproduction of timing and rendering results. Timings below are observations from this run, not benchmark guarantees.

> **Note:** during the session the shared browser window was resized to 768×415 and reloaded from outside the audit. I restored the viewport before the desktop checks, and I report the 768 px observations as tablet findings.

---

## 3. Data fidelity: aecs4u-stats vs. what the map shows

### 3.1 Feature-set comparison

| Location / zoom | Layer | DB features in viewport | Rendered (unique IDs) | Result |
|---|---|---|---|---|
| Palermo z12 | Administrative boundaries (`geo.geo_boundary`) | 12 | 12 | ✅ exact ID match |
| Palermo z12 | OMI market zones (`spatial.market_zone`) | 71 | 71 | ✅ exact ID match |
| Palermo z12 | Postal zones (`spatial.postal_zone`) | 34 | 34 | ✅ exact ID match |
| Palermo z12 | Municipality profiles | 9 | 9 names | ✅ (after ~10 s cold load) |
| Palermo z12 | Points of interest (`facts.poi`) | 1,042 | 1,041 (+10 edge-buffer extras) | ⚠️ 1 not returned by `queryRenderedFeatures`; it lies exactly on a tile edge (x = 2199.99996) and its tile loaded normally, so this is probably not a visible gap |
| Palermo z12 | Hazard areas (`spatial.hazard_area`) | 238 | 221 | ❌ 17 missing, **all** in tile `12/2199/1579`, which returned 503 |
| Palermo z12 | Cadastral parcels / sheets / urban sections | **0 / 0 / 0** | 0 | ⚠️ correct, but there is no data here (see H3) |
| Verona z16 | Cadastral parcels (`spatial.cadastral_parcel`) | 935 | 935 | ✅ exact ID match |
| Palermo z12 | Live POI layer (`/api/v1/enrichment/pois/`) | 25 within 2 km | **0** | ❌ contract bug (see C4) |

### 3.2 Attribute spot-checks

- **Parcel `L781A012400.147`** (id 5226175): `parcel=147`, `sheet=124`, `municipality_id=11180` and `source_release=cadastral.veneto.IT.duckdb` match the DB row.
  - `area_sqm` and `national_cadastral_reference` are **NULL for all 106,788 Verona parcels**. The detail panel therefore shows *Area: —*, even though the geometry gives 7,519 m².
- **Municipality profile Palermo**: `istat_code 82053`, `observation_count 377,732` and `market_zone_count 51` match.
- **POI sample** `347662 "La Cuba"` and **hazard sample** `1009050 landslide "Molto elevata P4"`: properties are carried through intact.

### 3.3 Coverage in aecs4u-stats

- `spatial.cadastral_parcel` extent is roughly **10.62–13.10 °E, 44.79–46.68 °N**. That is Veneto only, from `source_release cadastral.veneto.IT.duckdb`.
- The audited Palermo view has 0 parcels, 0 sheets and 0 urban sections.
- The other layers (OMI, postal, hazards, POI, boundaries, profiles) have national coverage.

### 3.4 Tile budget check

Parcel z14 tiles around central Verona hold 997–3,912 features. That is under the `max_features=5000` `LIMIT`, so tiles are not truncated there. Compressed size is about 120 KB, uncompressed about 265 KB. Note that the `LIMIT` in `read_mvt` has no `ORDER BY`, so any tile over the cap would drop an arbitrary subset.

---

## 4. Findings

Each finding lists **evidence**, **location** and a **suggested fix**.

### Critical

#### C1 — Parcel selection times out for most parcels (missing index)

- **Evidence:**
  - Clicking parcel `L781A012400.147` left the status on "Loading parcel…" for about 10 s. Then `/api/v1/enrichment/parcel/by-reference/L781A012400.147` returned **503** and the detail panel never opened ([08](map-audit-2026-09-26/08-parcel-click-result.png) shows the successful case, for contrast).
  - `EXPLAIN ANALYZE` of the lookup ran a **Seq Scan over 4,683,749 rows in 71.6 s**. The pool's `statement_timeout` is 8 s (`map_layers.py:177`).
  - A different parcel (`L781B016200.STRADA300`) resolved in under 1 s only because its row sits early in the heap. Behaviour depends on where the row happens to live.
- **Location:**
  - `map_layers.py:411` (`WHERE national_cadastral_reference = %s OR canonical_reference = %s`).
  - The same predicate appears in `read_adjacent_features` (`map_layers.py:~447`).
  - `spatial.cadastral_parcel` only has indexes on `pkey`, `geom`, `inspire_localid`, `municipality`, `release` and `source_key`.
- **Impact:**
  - Affects parcel click, `?parcel=` deep links, attribute-table row click, "Find adjacent parcels" and exact-reference search.
  - Any arbitrary string in `?parcel=` or `/by-reference/{x}` costs the server a full 8 s scan, which is a cheap load-amplification vector.
- **Fix:**
  - In aecs4u-stats, add `CREATE INDEX … ON spatial.cadastral_parcel (canonical_reference)` and `… (national_cadastral_reference)`, or rewrite the query as `UNION ALL` of two indexed lookups.
  - Client side: a click already has the feature `id` from the vector tile, so fetch `/api/v1/map/layers/cadastral-parcels/features/{id}` (≈1 s, PK lookup) instead of re-resolving by reference. *(Implemented differently: an `?id=` hint on `/parcel/by-reference`; see §8.)*

#### C2 — Map popups are unreadable in dark theme

- **Evidence:**
  - Clicking the map in dark mode opens a popup whose text is `rgb(248,250,252)` on a white `.maplibregl-popup-content`. That is a contrast ratio of **≈1.04:1**.
  - The close button is white on white as well ([03](map-audit-2026-09-26/03-click-z12.png)).
- **Location:** every `new maplibregl.Popup` in `map-v2.js` (e.g. `:746`, `:1156`, `:1252`, `:1468`, `:1521`, `:1638`). Popup content inherits `--color-text-primary` from `[data-bs-theme=dark]`, but MapLibre's own stylesheet keeps the white background.
- **Fix:** theme the popup in `map-v2.css`:
  - `.maplibregl-popup-content { background: var(--direct-map-panel); color: var(--direct-map-text); }`
  - Matching `.maplibregl-popup-tip` border colours per anchor.
  - `.maplibregl-popup-close-button { color: var(--direct-map-muted); }`
  - `.map-popup-label` hard-codes `#64748b`; use the muted token instead.

#### C3 — The attribute table renders underneath the map and cannot be closed by mouse

- **Evidence:**
  - After clicking "Attribute table" the toggle shows as active, but nothing appears ([18](map-audit-2026-09-26/18-attribute-table.png)).
  - The card's computed `position` is `static`, and `elementFromPoint` over the card returns `maplibregl-canvas`.
  - Playwright could not click `#mapTableCloseButton` because `#locateButton` intercepts the pointer.
- **Location:** `map-v2.css:28`. The shared `position: absolute; z-index: 2` rule lists every floating card **except `.map-table-card`**, so its `left/right/bottom` offsets (`:94`) do nothing.
- **Fix:** add `.map-table-card` to the selector at `:28` (with `z-index: 3`), and add Escape-to-close. Escape currently does not close it.

#### C4 — Live "Points of Interest" layer is always empty with the default categories

- **Evidence:**
  - With all ten categories selected (the default), `GET /api/v1/enrichment/pois/?…&categories=universities,schools,…` returns `total: 0`.
  - The same request with no `categories` returns **25**, and `categories=supermarkets` returns 8.
  - Any comma-separated pair returns 0. Repeated params (`categories=schools&categories=supermarkets`) return 13.
- **Location:**
  - `routers/enrichment.py:116` declares `categories: Optional[List[str]]` (repeated query params).
  - `map-v2.js:1306` sends `categories.join(',')`, which arrives as one unknown code `"a,b,…"`, so `c.code IN ('a,b,…')` matches nothing.
  - `poi-report` and the explorer use `_poi_category_query()` (`enrichment.py:133`), which *does* split on commas. The three endpoints disagree.
- **Fix:** make `/pois/` accept the same comma form via `_poi_category_query`, or send `params.append('categories', c)` per category. Add a contract test.

### High

#### H1 — Municipality search only matches exact names (SQL parameters bound in the wrong order)

- **Evidence:**
  - `?query=Palermo` finds Palermo.
  - `?query=Palerm`, `alermo`, `Monte` and `San ` all return **0** canonical results.
  - Only ISTAT-code prefixes do partial matching (`820` → 20 municipalities).
- **Location:** `map_layers.py:573`. The SQL is `canonical_name ILIKE $1 OR istat_code ILIKE $2 ORDER BY lower(name)=lower($3), lower(name) LIKE lower($4)`, but it is called with `(normalized, prefix, pattern, pattern)`. So `$1` has no wildcards, and the ORDER BY compares against `%…%`.
- **Fix:** bind `(pattern, prefix, normalized, prefix, limit)`. Add a regression test for partial names.

#### H2 — Failed tiles leave permanent, silent data holes

- **Evidence:**
  - On first load, hazard tiles `12/2200/1579`, `12/2199/1578` and `12/2199/1579` returned **503** because the cold query exceeded the timeout. Re-requested moments later, `12/2199/1579` served 9.1 KB in 7 ms (the other two were not re-measured).
  - MapLibre does not retry failed tiles. All 17 missing hazard polygons lie in `12/2199/1579`. (The one POI not found in §3.1 is unrelated: its tile loaded normally.)
  - The status pill kept saying "Map ready".
  - `/api/v1/map/metrics` over the session: **76 of 364 vector tiles took >1 s, 39 took 3–10 s, 5 errored**. Examples: `market-zones z13` 7.3 s, `cadastral-parcels z15` 5.0 s, `urban-sections z13` 2.5 s. `layer_health` hit its 5 s client abort on first load.
- **Location:** `routers/api.py:~4462-4467` (503 on any exception), `map_layers.py:291-326`.
- **Fix:**
  - Warm or pre-generate low-zoom tiles for national layers.
  - Return retryable failures with `Retry-After` and add a `map.on('error')` handler that reloads the source tile once and surfaces "Some tiles failed to load — Retry" in the status pill.
  - Make sure error responses are never cached (the success path sends `max-age=3600`).

#### H3 — The requested area has no cadastral data, and the UI never says so

- **Evidence:**
  - At the audited URL the DB has **0 parcels / 0 sheets / 0 urban sections**, because parcel coverage is Veneto only (§3.3).
  - The page shows "Zoom in to see cadastral parcels" ([01](map-audit-2026-09-26/01-desktop-initial.png)), and the layer list marks "Cadastral parcels" as **Available**.
  - The only hint is the generic "Some layers have partial regional coverage." A user who zooms in finds an empty map.
- **Fix:**
  - Publish a coverage extent (or per-region flag) in the layer catalog/health.
  - When the viewport is outside it, replace the help note with "No cadastral parcels published for this area yet (coverage: Veneto)".
  - "Available" in the layer list currently means "the relation exists". Rename it or drop it.

#### H4 — Parcel detail shows raw IDs and empty key values

- **Evidence:**
  - The panel subtitle and the *Municipality* row show `11180`, not "Verona".
  - *Area* shows "—" because `area_sqm` is NULL for every Verona parcel.
  - *National reference* is NULL throughout, so the national-format workflows (search, adjacency self-exclusion, enrichment) degrade.
- **Location:** `map-v2.js:~561` (`renderParcelPanel`); the gap is in aecs4u-stats `spatial.cadastral_parcel`.
- **Fix:**
  - Join `geo.geo_unit.canonical_name` in `read_feature_by_reference` / `read_feature_details`.
  - Fall back to `ST_Area(geom::geography)` when `area_sqm` is NULL, labelled "computed".
  - Backfill `area_sqm` and `national_cadastral_reference` in the Veneto load.

#### H5 — "Loading optional enrichment…" never resolves

- **Evidence:** `/api/v1/enrichment/parcel/details/L781B016200.STRADA300` returns 404 ("No enrichment data found"). The panel keeps the loading text indefinitely.
- **Location:** `map-v2.js:850`, where `if (!response.ok) return;` leaves the placeholder in place.
- **Fix:** on a non-OK response, render "No enrichment available for this parcel" (404) or "Enrichment unavailable — retry" (5xx).

#### H6 — Parcel references are not unique, so reference-based selection is ambiguous *(revised during remediation)*

- **Evidence:**
  - `canonical_reference` is not unique in `spatial.cadastral_parcel`. Verona has 106,788 rows but only 102,453 distinct references. Road and water pseudo-parcels are split across many polygons; `L781B016200.STRADA300` alone is 10 rows.
  - Every by-reference lookup ends in `LIMIT 1` with no `ORDER BY`, so it returns an **arbitrary** polygon. A click on one piece of a road could highlight and fly to another piece.
  - The audit first reported that "Find adjacent parcels" listed the selected parcel itself. On re-check, the listed `STRADA300` was a different polygon with the same reference, i.e. a genuine neighbour.
  - Separately, the old self-exclusion (`national_cadastral_reference IS DISTINCT FROM ref`) could never exclude anything for Veneto, where that column is always NULL.
- **Location:** `map_layers.py` (`read_feature_by_reference`, `read_adjacent_features`).
- **Fix (partly applied, see §8):**
  - Clicks carry the tile feature's primary key, and both lookups resolve the target by `id`.
  - Adjacency excludes the target by `id`.
  - **Still open:** reference-only entry points (deep links, search) remain ambiguous for duplicated references. Deep links should carry the `id`.

#### H7 — Administrative-boundary interaction reports the wrong unit, without a name

- **Evidence:**
  - All four hierarchy levels (ripartizione *Isole*, region *Sicilia*, province *Palermo*, 9 municipalities) are drawn in one layer with no filter.
  - A click in the city reported **province id 107 / geo_unit 8135**, with no name and no unit type.
  - Stacked fills accumulate (4 × 0.075 opacity), so the land tint is uneven by hierarchy depth.
- **Location:** `geo-boundaries` spec (`map_layers.py`, properties `id, geo_unit_id, generalization, source_release`). `generalization` is NULL in the DB.
- **Fix:**
  - Add `unit_type` and `canonical_name` (via `geo.geo_unit`) to the tile properties.
  - Filter or style by level per zoom (for example region <z8, province z8–10, municipality ≥z10).
  - Show the name in popups and resolve clicks to the most specific unit.

#### H8 — Search-as-you-type sends queries to public Nominatim

- **Evidence:**
  - Each debounced keystroke query also goes to `https://nominatim.openstreetmap.org/search?…&q=<text>`, including parcel references (`L781B016200.14`).
  - The results are labelled **"Municipality"** even for streets ("Via Zara 12/14 … Brescia", "Scolo Veron …") ([11](map-audit-2026-09-26/11-search-results.png)).
- **Location:** `map-v2.js:928`, `:882`.
- **Why it matters:**
  - The OSMF Nominatim usage policy forbids autocomplete and requires an identifying UA/Referer.
  - Typed queries leave the platform.
  - Mislabelling confuses users.
- **Fix:**
  - Only call a geocoder on explicit submit, or use a self-hosted or commercial geocoder.
  - Never forward strings that look like parcel references.
  - Label results by OSM `type`/`addresstype`.

### Medium

| ID | Finding | Evidence / location | Suggested fix |
|---|---|---|---|
| M1 | **Attribute-table race and truncation** | `loadTableData()` (`map-v2.js:1741`) never aborts earlier requests. The default layer is *Administrative boundaries*, so its late response can overwrite the parcel data. Observed: status "4 feature(s) in view" while the body said "No features in the current view". Parcels are capped at **500 of 935** in view ("(truncated)"). Rows have no `tabindex`/role. | Use an `AbortController` and a request token; default to parcels at z≥14; paginate server-side; make rows focusable buttons. |
| M2 | **Search keyboard support is incomplete** | ArrowDown from the input does not move into the listbox (no `aria-activedescendant`). Enter with results open does nothing. After selecting a result or clearing a selection, focus drops to `<body>`. Choosing "Verona" at z17 recenters but keeps z17 instead of fitting the municipality. | Implement the ARIA combobox pattern; Enter selects the highlighted or first option; return focus to the input; fit to the municipality bbox. |
| M3 | **Canonical parcel references cannot be searched** | `L781B016200.14`, exactly as displayed in the panel, returns "No result". The backend only recognises references containing `_` and `.` or the `A123 12 34` form (`routers/api.py:~4274-4301`). | Accept the canonical form `^[A-Z]\d{3}[A-Z]?\d{4}\d{2}\.\w+$` (needs the C1 index). |
| M4 | **Overlapping controls** | **Desktop:** an open Layers panel (x 1128–1420) covers the utility stack (x 1387–1425) ([02](map-audit-2026-09-26/02-layers-panel.png)); the help note covers and crops the Esri/OSM attribution ([14](map-audit-2026-09-26/14-light-theme.png)). **Tablet 768 px:** the parcel card overlaps the search card, Layers button and utility stack. **Mobile 375 px:** the Layers toggle (261–363 × 276–320) covers the Google Maps and Street View buttons (stack 322–360 × 78–341); the status pill sits on the attribution; "Sign In" wraps to two lines ([15](map-audit-2026-09-26/15-mobile.png)). | Reserve a right-hand gutter for the stack (panel `right: 65px`); move the help note above the attribution; add a 701–1024 px breakpoint; on mobile put Layers/Shortlist in a bottom bar. |
| M5 | **Shortlist occupies prime space for signed-out users** | A 360×260 px card with four inert filters and a "Status" select that only contains "All" is always open on desktop, and it can't be collapsed (`.shortlist-close-button { display:none }`). It also hides map content, including where search results expand. | Collapse to a "Sign in to use your shortlist" chip when signed out; allow collapse on desktop. |
| M6 | **Sign-in-gated and unconfigured features look enabled** | *Sales* toggles to `aria-pressed=true` and shows "Sales feed requires sign-in" (401). `/api/v1/sales/geo-layers` returns **503 on every page load**, which is the only console error. | Disable gated toggles with a sign-in hint; skip the geo-layers call when `salesGeoLayersUrl` isn't configured. |
| M7 | **Theme inconsistencies** | Dark UI over a light basemap. `localStorage` holds two disagreeing keys (`theme: "dark"`, `cadastre_dark_mode: "false"`). Dark mode also tints boundary lines poorly. | Couple basemap to theme (or offer it as the default choice); drop the legacy key. |
| M8 | **No basemap context at parcel zoom** | Basemap layers use `maxzoom = source max + 1 = 17` (deliberate, `map-v2.js:~152`). At z≥17, where parcels are labelled and selected, the map is parcels on a flat `#f2f2ef` background with no streets or buildings ([13](map-audit-2026-09-26/13-verona-centroid-after-wait.png)), and the Esri attribution disappears. | Use a basemap with native z19–20 (CARTO path already exists behind `cartoEnabled`, OSM raster, or a vector basemap), or keep the blurred raster up to z19 at reduced opacity. |
| M9 | **Cold-load latency and no loading affordance** | Tiles at z17 took several seconds, with visibly partial parcel coverage and hard tile edges mid-load ([12](map-audit-2026-09-26/12-search-verona.png) vs [13](map-audit-2026-09-26/13-verona-centroid-after-wait.png)). POI enrichment took 5.0 s. The status says "Map ready" throughout. | Show a "Loading tiles…" indicator driven by `dataloading`/`idle`; add tile caching (see H2). |
| M10 | **Mixed languages in an English UI** | POI categories are in Italian (Università, Scuole, Asili…). The bulletin legend is Italian (Allerta rossa…). The language button's accessible name is "Seleziona lingua: English". | Route these strings through `_()` / the JS i18n catalog. |
| M11 | **Layer z-order follows toggle order, not meaning** | Parcels (the primary layer) are drawn beneath the boundary, OMI, postal and hazard fills. Municipality-profile fills are drawn above POI circles. Order: `parcels < geo-boundaries < market-zones < postal < hazard < POI < municipality-profiles`. | Define a fixed stack: basemap → area fills → lines → parcels → points → labels → selection. Insert with `beforeId`. |
| M12 | **Civil Protection "no alert" zones tint the whole map** | With the bulletin on, green "Nessuna allerta" polygons wash over all land ([19](map-audit-2026-09-26/19-live-layers.png)), reducing contrast for every other layer. | Don't fill "no alert" zones (outline only), or use very low opacity. |

### Low

| ID | Finding | Evidence / location | Suggested fix |
|---|---|---|---|
| L1 | Render-blocking legacy scripts | Leaflet 1.9.4, Leaflet.VectorGrid and `map-v2.js` load synchronously on every visit, though Leaflet is only the no-WebGL fallback (`theme_overrides/map_v2.html`). | `defer` everything; lazy-load Leaflet only when WebGL init fails. |
| L2 | Font preload warning | "Inter/Lexend … preloaded but not used within a few seconds". | Fix `as=` or remove the preload. |
| L3 | Small text and hit targets | 10.5 px form labels and legend text, 10 px badges. Targets under 24 px: shortlist close 19×23, hazard checkbox 13×13, popup close 19×22, "Sign in" link 35×13. Dark-mode "Sign in" link contrast is 3.76:1. | Use a 12 px minimum and 24×24 px targets; raise link contrast. |
| L4 | Tab order ≠ visual order | Tab reaches the map canvas, fullscreen, zoom, compass and attribution **before** the search box, which is visually first. Sidebar group headers appear unnamed in the accessibility snapshot. | Move the search markup before `#directMap`; name the group buttons. |
| L5 | No keyboard path to select a parcel | The map pans and zooms with keys, but parcels can only be picked by pointer. The table rows that could serve this purpose are not focusable (M1). | Make table rows actionable, or add "select parcel at map centre" on Enter. |
| L6 | URL handling gives no feedback | `?lat=51.5&lng=-0.12` (London) silently becomes Rome at z12. An invalid `?parcel=` shows "Loading parcel…" (and costs a DB scan, see C1), then the parameter is dropped. | Toast "Location outside Italy — showing Rome"; validate the reference format client-side. |
| L7 | Public diagnostics | `/api/v1/map/layers/health` exposes schema/table names and row estimates, and `/api/v1/map/metrics` is unauthenticated. | Gate behind admin/auth or strip the internals in production. |
| L8 | First search after start returned empty | The first `?query=palermo` returned 0 results in 1.58 s; identical later calls returned Palermo in about 13 ms. The error is swallowed as an empty result. | Distinguish "no match" from "backend error" in the response and the UI. |
| L9 | Street/water pseudo-parcels dominate labels | `STRADA300`, `ACQUA100`… are labelled like real parcels at z17. | Style `STRADA*`/`ACQUA*` differently (no fill, muted label). |
| L10 | Selection feedback is transient | "Selection cleared", "Map link copied" and similar persist in the pill until the next event; there is no auto-dismiss. | Auto-clear non-error messages after ~4 s. |

---

## 5. What works well

- **Data fidelity on successful tile loads:** rendered IDs match aecs4u-stats exactly for the compared layers (§3.1). Attributes pass through unmodified, and SRIDs are consistent (4326 everywhere checked). Failed tiles caused the documented omissions.
- **Fast shell:** TTFB 12 ms, FCP 448 ms, DOMContentLoaded 546 ms, HTML 6 KB, 434 DOM nodes.
- **Tile transport:** gzip (~2.2× on parcel tiles), `Cache-Control: public, max-age=3600`, `Vary: Accept-Encoding`. Invalid coordinates return 400 and unknown layers 404. No per-tile truncation in dense Verona at z14.
- **Security headers:** HSTS, CSP, `X-Frame-Options: DENY`, `nosniff`, COOP/CORP. A `?parcel=<img onerror>` payload was not executed.
- **Parcel labels** render with self-hosted Noto glyphs (no third-party font host), and hide below z17.
- **Accessibility basics:** skip link, a single `h1`, `aria-live` status and search regions, labelled icon buttons, keyboard pan/zoom on the canvas, visible focus rings on all tabbable controls.
- **URL state:** view and selection are serialized to the URL, zoom is clamped to 22, and non-numeric coordinates fall back to the national view.
- **Adjacency** returns sensible neighbours; note that duplicated references mean a road can list its own other segments (H6). Copy-link works, and the Civil Protection bulletin (187 zones) and auction layer (6) load.

---

## 6. Remaining work

1. Give interactive lookups (municipality and parcel search, parcel detail) their own small connection pool, or a longer timeout, so they don't queue behind tile rendering. With the shared 8-connection pool, municipality search returned 503 during verification while the database was under load (§8.1).
2. Apply the reference-index draw order at initial load, not only after an overlay toggle (M11).
3. Keep the coverage help note clear of the attribution at desktop widths (M4), and stop "Sign In" wrapping on mobile.
4. Localize the language button's accessible name (M10), and keep the mobile sidebar toggle's `aria-expanded` in sync with the drawer (also reported in `playwright-audit.md`).
5. Backfill `area_sqm` and `national_cadastral_reference` for the Veneto parcel load (H4). A full-table `area_sqm` update from another session was running during verification.
6. Make reference-only parcel links unambiguous where a reference maps to several polygons (H6).
7. Consider warming frequently requested low-zoom national tiles (H2).
8. Fix the font preload warning (L2) in aecs4u-theme.

## 7. Appendix: reproduction snippets

```bash
# C1 — reference lookup cost
psql -d aecs4u-stats -c "EXPLAIN ANALYZE SELECT id FROM spatial.cadastral_parcel
  WHERE national_cadastral_reference='L781A012400.147' OR canonical_reference='L781A012400.147' LIMIT 1"

# C4 — POI category contract
curl "localhost:8011/api/v1/enrichment/pois/?lat=38.090526&lng=13.392334&radius_km=2"                                  # 25
curl "localhost:8011/api/v1/enrichment/pois/?lat=38.090526&lng=13.392334&radius_km=2&categories=schools,supermarkets"  # 0
curl "localhost:8011/api/v1/enrichment/pois/?lat=38.090526&lng=13.392334&radius_km=2&categories=schools&categories=supermarkets"  # 13

# H1 — partial municipality names
curl "localhost:8011/api/v1/map/search?query=Palerm"   # results: []

# H6 — this reference identifies multiple polygon features, so a reference-only
# lookup is ambiguous. The audit found 10 polygons with this reference.
psql -d aecs4u-stats -c "SELECT canonical_reference, count(*) FROM spatial.cadastral_parcel WHERE canonical_reference='L781B016200.STRADA300' GROUP BY canonical_reference"
```

```js
// Playwright init script used to reach the MapLibre instance without app changes
Object.defineProperty(window, 'maplibregl', { configurable: true,
  get() { return this._ml; },
  set(v) { const O = v.Map; v.Map = class extends O { constructor(o) { super(o); window.__auditMap = this; } }; this._ml = v; } });
```

---

## 8. Remediation log

### Status key

- **Open / unresolved:** implementation work remains, or the reported issue still needs investigation.
- **Partly fixed:** the main path is changed, but a related case remains open.
- **Implemented; browser verification pending:** code was changed, but the post-fix UI was not re-checked in Playwright.
- **Fixed / implemented; verified:** the stated contract was checked in-process or by a unit test; this does not imply browser verification.

These changes were applied on 2026-09-26 on top of the uncommitted `feature/map-srs-implementation` work tree; nothing has been committed. The focused contract tests pass (50 passed), and JavaScript syntax and Python compilation checks pass. The aecs4u-stats index migration was applied, and the UI was re-checked in a browser; see §8.1.

| Finding | Change | Status and verification |
|---|---|---|
| C1 | `read_feature_by_reference` and `read_adjacent_features` accept `feature_id` and resolve the parcel by primary key (still checking that the reference matches). `/parcel/by-reference/{ref}` and `/parcel/adjacent/{ref}` take `?id=`. `map-v2.js` passes the clicked tile feature's `id`; URL state carries the selected id. `scripts/sql/map-reference-indexes.sql` adds concurrent btree indexes on both reference columns. | **Fixed; verified.** Indexes applied on 2026-09-26 with `CREATE INDEX CONCURRENTLY` (~17 min; the build waited for two long-running queries from another client), followed by `ANALYZE`: the table had never been analyzed, so the planner kept choosing a full primary-key walk. Lookups now use a `BitmapOr` over both indexes. Browser: a parcel click resolves via `?id=` (2.6 s while another session's full-table update was running). Reference-only links to duplicated references remain ambiguous (H6). |
| C2 | `map-v2.css`: popup content, tip, and close button use the panel/text/muted tokens; `.map-popup-label` and the sales popup divider use tokens. | **Fixed; browser-verified.** Dark-mode popup text 18.6:1, close button 13.1:1 and 24×24 px. |
| C3 | `.map-table-card` added to the absolute-positioning rule (`z-index: 3`, `right: 60px` clear of the zoom controls). Escape closes the table and returns focus to the toggle. | **Fixed; browser-verified.** The table renders on top, Escape closes it and returns focus to the toggle, and the close button is clickable. Its pagination row was hidden under the signed-out shortlist chip; fixed during verification (§8.1). |
| C4 | `/enrichment/pois/` splits comma-separated `categories` values. | **Fixed; verified.** Unit test added. In-process against aecs4u-stats, the full ten-category comma list returns 25 POIs at the audited point (0 before the fix). |
| H1 | `search_municipalities` binds `(pattern, prefix, exact, prefix)`. A later H7 change made `_source_columns` drop `canonical_name` from every layer, so results were labelled with ISTAT codes (`Roma` → `58091`, …); `_source_columns` now strips it only for `geo-boundaries`. | **Fixed; verified against the live API.** `Roma` → Roma, Romagnano al Monte, …; `Palerm` → Palermiti, Palermo; municipality-profile tiles carry `canonical_name` again. Regression tests check the generated columns and search SQL. Browser: “Palerm” lists Palermiti and Palermo when search responds (see §8.1 for 503s under load). |
| H2 | Tile failures return `Retry-After: 2` and `Cache-Control: no-store`; the client retries failed non-basemap tile sources (up to 2× per view, backing off), then reports “Some map data failed to load; pan or zoom to retry.” | **Partly fixed; retry browser-verified.** With the first request for a hazard tile forced to 503, the tile was re-requested and the rendered count matched a clean run (222 and 222). Tile warming remains open. |
| H3 | Cadastral catalog entries expose Veneto coverage notes and bounds. Outside the published bounding box, the help note says “No cadastral parcels published for this area yet (coverage: Veneto).” Layer rows identify partial coverage. | **Fixed; browser-verified.** At the audited Palermo view the help note reads “No cadastral parcels published for this area yet (coverage: Veneto).” The bounds are an approximate extent, not a coverage polygon. |
| H4 | Reference lookups and parcel details return `municipality_name` (via `geo.geo_unit`) and `computed_area_sqm` (`ST_Area(geom::geography)`); the panel uses the computed area and labels it “Area (computed)”. | **Partly fixed; display browser-verified.** The panel shows “Verona” and “Area (computed) 656,7 m²”. Still open: backfilling `area_sqm` and `national_cadastral_reference` in aecs4u-stats. |
| H5 | A failed or 404 enrichment request replaces the placeholder with “No optional enrichment is available…” or “temporarily unavailable”. | **Fixed; browser-verified.** After the details endpoint returns 404, the panel reads “No optional enrichment is available for this parcel.” |
| H6 | Click and adjacency flows identify the selected polygon by primary key; selection URLs retain the id, and ambiguous exact-reference searches return the matching polygons for the user to choose. | **Partly fixed; browser-verified for clicks.** The URL carries `parcel_id`, and adjacency (11 neighbours) excludes the selected polygon. Reference-only links to duplicated references remain ambiguous. |
| H7 | Boundary tiles include `canonical_name` and `unit_type` via `geo.geo_unit`; rendering filters hierarchy levels by zoom and popup titles use the unit type/name. | **Fixed; browser-verified.** At z12 only municipality boundaries render, and the popup is titled “municipality: Palermo”. |
| H8 | Nominatim is called only on explicit submit (Enter/button), never while typing. Results are labelled by OSM type. Parcel-looking strings are not forwarded to Nominatim. | **Fixed; browser-verified.** 0 Nominatim requests while typing, 1 on an explicit submit, 0 for a parcel-shaped submit. |
| M1 | The attribute table aborts superseded requests, defaults to parcels at parcel zoom, makes rows keyboard actionable, and pages through server-side results using offsets. | **Fixed; browser-verified.** Defaults to parcels at z16 with 100 rows per page, and Next moves from “Page 1+” to “Page 2+”. The pagination row was covered by the shortlist chip and pushed out of view; fixed during verification (§8.1). |
| M2 | The search uses an ARIA combobox/listbox with active-descendant navigation; Enter selects a result, focus returns to the input after selection/clear, and municipality results use their bounding box. | **Fixed; browser-verified when search responds.** ArrowDown sets `aria-activedescendant`, Enter selects, focus stays on the input, the view fits the municipality, and focus returns to the input after Clear selection. Later runs hit search 503s under database load (§8.1). |
| M3 | `/map/search` accepts canonical parcel references, returns matching feature ids, and avoids municipality/geocoder lookup for parcel-shaped input. | **Fixed; browser-verified.** `L781B016200.14` returns “L781B016200.14 · Verona · Sheet 162”. This needed the C1 indexes, `ANALYZE`, and a query rewrite (a `UNION` of two indexed lookups) so stale statistics cannot force a full scan. |
| M4 | Desktop panels reserve space from the utility stack; tablet layout has a 701–1024 px breakpoint; mobile Layers and Shortlist controls share a bottom action bar. The themed `/map` template now uses the same action bar as the fallback template. | **Partly fixed; browser-checked.** No overlaps at 768 px or 375 px, and the mobile Layers control works. Still open: at 1440×900 the coverage help note overlaps the attribution by about 20 px, and “Sign In” wraps to two lines on mobile (55×54 px). |
| M5 | Signed-out users see a shortlist sign-in chip and not the empty card; signed-in users can collapse/reopen the shortlist. | **Fixed; browser-verified.** Signed out, the empty card is hidden and the “Sign in to use your shortlist” chip is shown. |
| M6 | Sales is disabled for signed-out users. The sales geo-layer request is emitted only when `LAND_REGISTRY_SALES_GEO_LAYERS_URL` is configured. | **Fixed; browser-verified.** The Sales toggle is disabled when signed out, and no `/sales/geo-layers` request is made. |
| M7 | Dark mode selects the dark basemap by default absent a saved choice; the legacy `cadastre_dark_mode` preference is removed. | **Fixed; browser-verified.** With the dark theme and no saved basemap, the Esri Dark Gray basemap loads. |
| M8 | Basemap raster layers remain visible through zoom 19 by upscaling the final native tile; the attribution remains associated with the layer. | **Fixed; browser-verified.** The basemap layer's `maxzoom` is 19, and the Esri attribution is present at z17. |
| M9 | Map loading status is driven by load/idle events; failed tiles surface a retry status. | **Fixed during verification; browser-verified.** The new idle handler formed a render loop (about 10 renders per second on a still map) and overwrote every status message within a frame. Now 0 renders in 3 s of a still map. |
| M10 | POI categories and bulletin labels use the page's `window._i18n` catalog. | **Partly fixed; browser-verified.** POI categories (Universities, Schools, …) and the bulletin legend (Red alert, …) are in English. Still open: the language button's accessible name is “Seleziona lingua: English”. |
| M11 | A fixed layer stack is reapplied after catalog loading and visibility changes, with parcels, points, labels and selection above area fills. | **Partly fixed; browser-checked.** After any overlay toggle the stack is correct (area fills below the parcel fill, then lines and labels), but at initial load `fill-geo-boundaries` is still drawn above the parcels. |
| M12 | “No alert” bulletin polygons use a near-transparent fill. | **Fixed during verification; browser-verified.** The L1 change had removed the TopoJSON parser from `/map`, so the bulletin drew 0 zones; restored. 13 zones drawn, and “no alert” zones use opacity 0.015. |
| L1 | MapLibre and the direct map bundle load with `defer`; Leaflet/VectorGrid are loaded only by the no-WebGL fallback. | **Fixed; browser-verified.** `window.L` is undefined on `/map`. The TopoJSON parser, which the bulletin needs, was restored (M12). |
| L2 | No change in this repo. The preload is in the aecs4u-theme package's `base.html` (line 39 of the rendered page: `rel="preload" as="style"` for Google Fonts, swapped to a stylesheet by script). | **Open; belongs in aecs4u-theme.** |
| L3 | Map controls, checkbox targets and small labels/badges have larger minimum sizes; popup contrast uses theme tokens. | **Fixed; browser-verified.** No visible target under 24 px in the map shell (the inline MapLibre attribution link is exempt). |
| L4 | Search markup precedes the map canvas, and layer-group controls are named. | **Fixed; browser-verified.** The search input precedes the map canvas in tab order. |
| L5 | Attribute-table rows can be selected with keyboard; selected parcel URLs preserve the feature id. | **Fixed; browser-verified.** Table rows have `tabindex=0`, and Enter opens the feature popup. |
| L6 | Out-of-country URL coordinates show a Rome fallback message; malformed parcel references are handled without silently leaving the loading state. | **Fixed; browser-verified.** London coordinates show “Location outside Italy — showing Rome.”; `?parcel=not-a-parcel` shows “Invalid parcel reference — selection was not loaded.” |
| L7 | Public health output omits physical table/geometry internals and row estimates; `/map/metrics` returns 404 unless `MAP_PUBLIC_METRICS` is explicitly enabled. | **Implemented; contract tests pass.** |
| L8 | Search backend exceptions return 503 instead of being treated as empty results; the frontend surfaces the service error. | **Fixed; browser-verified.** A 503 from search shows “Search unavailable”. |
| L9 | `STRADA*` and `ACQUA*` pseudo-parcel labels are filtered from the label layer. | **Fixed; browser-verified.** 0 STRADA/ACQUA labels among 214 parcel labels at z17. |
| L10 | Non-error status messages clear automatically after four seconds. | **Fixed during verification; browser-verified.** “Map link copied” shows and then clears, and “Map ready” clears after 4 s. Both were previously overwritten by the idle loop (M9). |

### 8.1 Post-migration browser verification

**Method.** Headless Chromium 151 via Playwright 1.63 with an isolated profile (the shared Playwright MCP browser was held by another session). Viewports were 1440×900 in dark and light themes, 768×1024 and 375×812, with an English locale. Cookie consent was pre-accepted, because on a fresh profile the consent modal covers the whole page. One scripted check per finding, compared with the database where applicable. Screenshots are in `.playwright-mcp/audit/post/` (ignored by git).

**Last full run:** 41 checks passed and 7 failed:

- **M10:** the language button name is still Italian.
- **M11:** the initial layer order is wrong until the first toggle.
- **M4:** the desktop help note overlaps the attribution, and “Sign In” wraps on mobile.
- **H1 and M2:** municipality search returned 503 under database load; both passed in an earlier run before the load began.
- **Console:** the browser logs the expected 404 for a parcel without an enrichment record.

**Regressions found and fixed during verification:**

1. **Municipality names (H1).** The H7 change made `_source_columns` drop `canonical_name` from every layer, so search results and profile tiles showed ISTAT codes. It is now stripped only for `geo-boundaries`.
2. **Civil Protection bulletin (M12).** The L1 script cleanup also removed the TopoJSON parser from `/map`, so the bulletin drew 0 zones and showed “(0)”. The script is restored, and a missing parser now reports an error instead of looking like “no alerts”.
3. **Idle render loop (M9, L10).** The `idle` handler re-applied layer visibility on every idle, which re-rendered the map about 10 times per second and replaced every status message with “Map ready” within a frame. It now acts only after a tile-loading phase, and visibility writes are skipped when unchanged.
4. **Table pagination (M1, C3).** The signed-out shortlist chip sat above the table's pagination row and intercepted clicks, and the nested scroll pushed pagination out of view. The table is now above the chip, and only its rows scroll.
5. **Parcel search plan (M3, C1).** Even with the new indexes, `spatial.cadastral_parcel` had never been analyzed, so the planner walked the primary key (a full scan) and the 2 s search timeout returned 503. The fix was `ANALYZE` (now part of `scripts/sql/map-reference-indexes.sql`) plus a `UNION` of two indexed lookups.

**Database changes applied to aecs4u-stats:** `cadastral_parcel_canonical_reference_idx`, `cadastral_parcel_national_reference_idx`, and `ANALYZE spatial.cadastral_parcel`.

**New finding: search fails while tiles load under database load.** Municipality search has a 1.5 s timeout and shares the 8-connection pool with tile rendering. During verification another session was running a full-table `UPDATE spatial.cadastral_parcel SET area_sqm = …`. In the browser, “Palerm” returned 503 twice before a 200, while the same request from `curl` succeeded 8 of 8 times in about 22 ms. Recommended fix: a separate small pool (or a higher timeout) for interactive lookups.

**Also observed:** the mobile sidebar toggle reports `aria-expanded="true"` while the drawer is off-screen (x = −375), matching `playwright-audit.md` P2.

**Remaining work:** see §6.
